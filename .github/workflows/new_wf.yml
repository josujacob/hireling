name: Workflow Run By Demo
on:
  repository_dispatch: 
    types: testing


jobs:
 build:
    runs-on: ubuntu-latest
    steps:
      - name: testing
        run: echo hello
      - name: printing
        run: whoami
      
